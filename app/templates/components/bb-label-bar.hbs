{{#each labels as |label|}}
  {{#if template.blockParams}}
      {{yield label}}
    {{else}}
      <rect x=0 y={{label.rectY}} width="40" height="20" rx="5"/>
      <text x=10 y={{label.y}}>{{label.text}}</text>
  {{/if}}
{{/each}}
